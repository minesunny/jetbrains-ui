---
title: Build
description: JetBrains Build 图标组件，支持多种尺寸和 Light/Dark 主题。
---

import { BuildIconsGallery } from '@/components/docs/icons/build/build-icons-gallery';

import { IconDetailPreview } from '@/components/docs/icons/icon-detail-preview';

## Overview

来自 JetBrains UI 设计系统的 **23 个 Build 图标**，以独立 React 组件形式提供，支持 TypeScript。

### Usage

<IconDetailPreview name="Build" category="build" />

```tsx
import { SVG } from '@/registry/components/svg';

<SVG name="build/build" />

<SVG name="build/build" size="lg" />

<SVG name="build/build" aria-label="Build icon" />
```

## Browse Icons

Click any icon to see import code and all size variants.

<BuildIconsGallery />
