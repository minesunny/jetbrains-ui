---
title: Tabs
description: Tabs 组件。
---

<Cards>
  {[{ title: 'Tabs', slug: 'base-tabs' }].map(({ title, slug }) => (
    <Card
      key={slug}
      href={`/docs/components/tabs/${slug}`}
      title={title}
      accent
    />
  ))}
</Cards>
