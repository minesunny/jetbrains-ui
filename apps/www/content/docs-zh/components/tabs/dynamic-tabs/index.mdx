---
title: Dynamic Tabs
description: 数据驱动的标签栏组件，支持右键菜单、关闭和固定。
author:
  name: minesunny
  url: https://github.com/minesunny
releaseDate: 2026-05-17
---

<ComponentPreview name="tabs-dynamic-tabs-demo" />

## 概述

`DynamicTabsList` 从 items 数组渲染数据驱动的标签栏。每个标签带有关闭/固定按钮和右键上下文菜单。内容渲染由消费者控制——组件只管理标签栏。

## 安装

<ComponentInstallation name="dynamic-tabs" />

## 用法

### 基础用法

```tsx
import { useState, useCallback, memo } from 'react';
import {
  DynamicTabsList,
  type DynamicTabItem,
} from '@/components/jetbrains-ui/tabs/dynamic-tabs';

const tabs: DynamicTabItem[] = [
  { id: 'main', label: 'main.ts', pinned: true },
  { id: 'app', label: 'app.tsx' },
  { id: 'utils', label: 'utils.ts' },
];

function Example() {
  const [items, setItems] = useState(tabs);
  const [activeId, setActiveId] = useState('main');

  const closeTabs = useCallback((ids: string[]) => {
    setItems((prev) => prev.filter((t) => !ids.includes(t.id)));
  }, []);

  const togglePin = useCallback((tabId: string) => {
    setItems((prev) =>
      prev.map((t) => (t.id === tabId ? { ...t, pinned: !t.pinned } : t)),
    );
  }, []);

  return (
    <>
      <DynamicTabsList
        items={items}
        activeTab={activeId}
        onActiveChange={setActiveId}
        closeTabs={closeTabs}
        togglePin={togglePin}
      />
      <div>
        {items.map((tab) => (
          <div
            key={tab.id}
            style={{ display: tab.id === activeId ? undefined : 'none' }}
          >
            <MemoizedContent tabId={tab.id} />
          </div>
        ))}
      </div>
    </>
  );
}
```

### 防止内容重复渲染

在内容组件上使用 `React.memo`，非活动标签使用 `display: none` 隐藏。这保持重型内容（编辑器、表格）始终挂载但隐藏，避免标签切换时的重复渲染：

```tsx
const MemoizedContent = memo(function Content({ tabId }: { tabId: string }) {
  return <Editor tabId={tabId} />;
});
```

### 禁用右键菜单

```tsx
<DynamicTabsList items={items} contextMenu={null} />
```

### 自定义右键菜单

```tsx
<DynamicTabsList
  items={items}
  contextMenu={(tab, index, actions) => (
    <>
      <ContextMenuItem onSelect={() => actions.close?.()}>
        自定义关闭
      </ContextMenuItem>
    </>
  )}
/>
```

## 右键菜单操作

| 操作            | 描述                     | 禁用条件                               |
| --------------- | ------------------------ | -------------------------------------- |
| 固定 / 取消固定 | 切换固定状态             | 未提供 `togglePin`                     |
| 关闭            | 关闭当前标签             | 标签已固定，或未提供 `closeTabs`       |
| 关闭其他        | 关闭其他所有未固定的标签 | 未提供 `closeTabs`                     |
| 关闭左侧标签    | 关闭左侧未固定的标签     | 左侧无未固定标签，或未提供 `closeTabs` |
| 关闭右侧标签    | 关闭右侧未固定的标签     | 右侧无未固定标签，或未提供 `closeTabs` |

所有关闭范围操作自动跳过已固定的标签。

## API 参考

### DynamicTabsList

<TypeTable
  type={{
    items: {
      description: '要渲染的标签项数组',
      type: 'DynamicTabItem[]',
      required: true,
    },
    activeTab: {
      description: '受控的活动标签 ID',
      type: 'string',
      required: false,
    },
    defaultActiveTab: {
      description: '默认活动标签 ID（非受控模式）',
      type: 'string',
      required: false,
    },
    onActiveChange: {
      description: '活动标签变更时的回调',
      type: '(tabId: string) => void',
      required: false,
    },
    closeTabs: {
      description: '关闭标签的回调。接收要关闭的 ID 数组。',
      type: '(tabIds: string[]) => void',
      required: false,
    },
    togglePin: {
      description: '切换标签固定状态的回调',
      type: '(tabId: string) => void',
      required: false,
    },
    contextMenu: {
      description: '自定义右键菜单渲染函数，传入 null 禁用',
      type: 'DynamicTabContextMenuFn | null',
      required: false,
      default: '默认右键菜单',
    },
  }}
/>

### DynamicTabItem

<TypeTable
  type={{
    id: {
      description: '标签的唯一标识符',
      type: 'string',
      required: true,
    },
    label: {
      description: '标签的显示文本',
      type: 'string',
      required: true,
    },
    icon: {
      description: '标签前显示的图标名称（来自 SVG 注册表）',
      type: 'string',
      required: false,
    },
    pinned: {
      description:
        '是否固定标签。固定的标签显示固定图标代替关闭按钮，且受保护不被关闭操作影响。',
      type: 'boolean',
      required: false,
      default: 'false',
    },
    disabled: {
      description: '是否禁用标签',
      type: 'boolean',
      required: false,
      default: 'false',
    },
  }}
/>
