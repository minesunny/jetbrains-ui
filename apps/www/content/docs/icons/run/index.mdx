---
title: Run
description: JetBrains Run icon components with multiple sizes and light/dark themes.
---

import { RunIconsGallery } from '@/components/docs/icons/run/run-icons-gallery';

import { IconDetailPreview } from '@/components/docs/icons/icon-detail-preview';

## Overview

A set of **46 Run icons** from the JetBrains UI design system, provided as standalone React components with TypeScript support.

### Usage

<IconDetailPreview name="run/failed-in-progress" category="run" />

```tsx
import { SVG } from '@/registry/components/svg';

<SVG name="run/failed-in-progress" />

<SVG name="run/failed-in-progress" size="lg" />

<SVG name="run/failed-in-progress" aria-label="FailedInProgress icon" />
```

## Browse Icons

Click any icon to see import code and all size variants.

<RunIconsGallery />
